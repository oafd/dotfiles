return {
  "nvim-treesitter/nvim-treesitter-context",
  opts = {
    enable = true,           -- enable the context window
    multiline_threshold = 1, -- collapse multi-line context
    separator = "─",         -- ← this draws the horizontal line
  },
}
